# 智能待办 · 桌面级待办事项应用

一款**免安装**的桌面级待办事项应用：**灵动岛悬浮窗**、**系统托盘后台常驻**、**按提醒风格自动安排提醒频率**。

> 本仓库为**源码仓库**（不含 66MB 的便携 exe，构建产物已在 `.gitignore` 中排除）。
> 想直接使用，请按下文自行构建，或从本项目的 Releases 下载。

## 快速开始

### 运行开发版

```bash
npm install
npm start
```

### 打包免安装便携版

```bash
npm run dist
# 产物：dist/智能待办-便携版-1.0.0.exe（单文件，解压即用，绿色免安装）
```

打包完成后双击该 exe 即可运行：

- 系统托盘出现应用图标，关闭窗口**不会退出**，而是**常驻后台**
- 屏幕右下角出现**灵动岛悬浮窗**
- 任务到期时以**应用内弹窗 + 灵动岛**提醒（不含系统级通知）
- 支持**开机自启**

> 若下载 Electron 或打包组件失败，仓库内 `.npmrc` 已配置国内镜像；
> 打包时若因代码签名组件下载超时，可设 `CSC_IDENTITY_AUTO_DISCOVERY=false` 跳过签名。

## 功能特性

| 特性 | 说明 |
|------|------|
| 🔕 无 Emoji 图标 | 全部使用内联 SVG 矢量图标 |
| 🖥 桌面级 | Electron，真正的 Windows 原生窗口/托盘 |
| 🪟 无边框窗口 | borderless 自绘标题栏，含最小化/最大化/关闭 |
| 🎭 后台常驻 | 关闭窗口驻留托盘，系统托盘双击恢复 |
| 🔔 应用内提醒 | **不含系统级通知**：以应用内弹窗 + 灵动岛提示提醒 |
| ⏳ 启动加载 | 页内品牌加载遮罩，数据就绪后淡出 |
| 🎨 主题色 | 7 种主题色可选，主窗口与灵动岛同步 |
| 🌗 跟随系统 | 外观可选「跟随系统 / 浅色 / 深色」，系统切换实时同步 |
| 🧊 灵动岛悬浮窗 | **默认开启**；胶囊显示最要紧任务，点击展开，无操作 6 秒自动收回 |
| 🧠 提醒风格 | 侧边栏「提醒风格」卡片，用大白话选择提醒频率 |
| 🗂 可自定义分类 | 分类可随时新增 / 重命名 / 删除，支持筛选 |
| 🚀 开机自启 | 一键设置开机自动启动（固定目录副本） |
| 📅 DDL 管理 | 截止日期 + 时间，自动计算倒计时 |
| 💾 数据持久化 | 存于本地 `todo-data.json`，重启不丢失 |
| ⬇️ 导出备份 | 一键导出 JSON |

## 提醒风格（本软件核心特色）

侧边栏顶部的「提醒风格」卡片点击后进入**专属覆盖界面**（不与其他设置混在一起）。
界面用生活化语言描述每种风格，并用时间线直观展示"会在什么时候提醒你"：

| 风格 | 适用心态 | 提醒次数 | 提醒时间点（相对截止） |
|------|---------|---------|----------------------|
| 拖延者 · 需要被催 | 我总拖着不动，请多提醒我几次 | 4 次 | 3 天前 / 1 天前 / 12 小时前 / 2 小时前 |
| 忙碌者 · 别烦我 | 我很忙，只在关键节点提醒我 | 2 次 | 1 天前 / 2 小时前 |
| 组织者 · 心里有数 | 我自己会安排，提醒一次就够 | 1 次 | 6 小时前 |
| 完美主义者 · 不能出错 | 我要提前很久就收到分阶段提醒 | 5 次 | 7 天前 / 3 天前 / 1 天前 / 6 小时前 / 1 小时前 |

进入方式：侧边栏「提醒风格」卡片，或 设置 → 提醒风格 → 更改风格。

## 设置与未保存提示

- 设置面板会跟踪「提醒提前量 / 声音提示 / 后台常驻」的改动，标题旁显示**「有未保存的更改」**标记。
- 未保存就关闭（点 ×、点遮罩、按 Esc）时会询问：**保存 / 放弃更改 / 取消**。
- 主题色与外观是即时生效的，无需保存。

| 完美主义者 | 5 次 | 提前 7天 / 3天 / 1天 / 6h / 1h |

## 开机自启与残留文件

便携版每次运行会自解压到 `%TEMP%`，因此：

- **开启自启时**：程序复制一份到固定目录 `%APPDATA%\SmartTodoDesktop\智能待办.exe`，
  并通过注册表 `HKCU\...\Run` 指向该固定副本；同时提示可能存在该副本文件。
- **取消自启时**：程序会删除注册表项，并**自动删除固定目录下的副本文件**，
  避免残留（若文件被占用删除失败，会提示手动删除）。

## 灵动岛悬浮窗

- **默认开启**（首次启动即出现在屏幕右下角）；可用操作栏「悬浮窗」按钮或托盘菜单开关，开关状态会被记住。
- **默认形态为药丸胶囊**（类似 iOS 灵动岛），只显示「今日 N 项」摘要，无窗口阴影。
- **点击药丸**即展开为卡片（药丸↔卡片带形状补间动画），展示今日到期任务列表；
  可按钉子图标切换置顶，按箭头收起回药丸。
- **通知联动**：当任务提醒触发时，悬浮窗自动展开并弹出渐变横幅，5 秒后自动收回药丸形态。
- **点击穿透**：窗口是固定尺寸的透明窗口，岛体之外的空白区会自动忽略鼠标事件，
  因此不会遮挡桌面点击；指针移入岛体即恢复可交互。
- 按钮提示为自绘 tooltip（不使用原生 `title`，避免白边）。
- 实现要点：窗口尺寸**固定不变**，切换只改 CSS 类并用 `width/height/border-radius` 过渡动画。
  这样绕开了「`resizable:false` 的窗口在 Windows 上无法用 `setSize` 改变大小」的问题。

## 启动加载

- 应用内显示品牌加载遮罩（Logo + 进度条），数据载入、首屏渲染完成后自动淡出。
- 页内遮罩是唯一的启动反馈（不使用额外的独立启动窗口）。

## 灵动岛悬浮窗

- **默认开启**（首次启动即出现在屏幕右下角）；可用操作栏「悬浮窗」按钮或托盘菜单开关，开关状态会被记住。
- **折叠态为药丸胶囊**（类似 iOS 灵动岛），显示最要紧的一条信息：
  有逾期时显示「逾期 N 项」+ 最急任务名；否则显示「今日 N 项」+ 最近一条任务名；
  都为空时显示「暂无待办」。任务名过长会自动截断，保证胶囊内一行显示。
- **点击药丸**即展开为卡片（药丸↔卡片带形状补间动画），展示逾期与今日任务列表；
  可按钉子图标切换置顶，按箭头收起回药丸。
- **无操作自动收回**：展开后若 6 秒内没有真实操作（指针移动、点击、滚轮、按键），
  会自动切回胶囊形态。指针原地不动产生的重复事件不会延长计时。
- **通知联动保持当前形态**：收到提醒时**不会强行改变**形态——
  - 当前是胶囊：蓝色圆内播放**线性（匀速）扫描光带 + 匀速旋转弧环**，图标切换为铃铛，
    文字上滑淡入显示提醒标题，5 秒后自动复原；
  - 当前是卡片：显示渐变横幅，5 秒后自动移除。
- **点击穿透**：窗口是固定尺寸的透明窗口，岛体之外的空白区会自动忽略鼠标事件，
  因此不会遮挡桌面点击；指针移入岛体即恢复可交互。
- 按钮提示为自绘 tooltip（不使用原生 `title`，避免白边）。
- 实现要点：窗口尺寸**固定不变**，切换只改 CSS 类并用 `width/height/border-radius` 过渡动画。
  这样绕开了「`resizable:false` 的窗口在 Windows 上无法用 `setSize` 改变大小」的问题。

## 提醒方式（不含系统级通知）

- 提醒以**应用内弹窗（toast）+ 灵动岛提示**呈现，不再调用 Windows 系统通知。
- 可在「设置 → 提醒方式」中开关声音提示。

## 无边框窗口

主窗口为 borderless（无系统边框），顶部使用自绘标题栏：

- 左侧显示应用名与版本号（主页左上角的大标题已移除，避免重复）
- 右侧为最小化 / 最大化（还原）/ 关闭按钮
- 关闭按钮遵循下方的关闭行为策略

## 关闭行为（首次询问）

首次点击关闭时弹出选择：

| 选择 | 行为 |
|------|------|
| 后台常驻 | 程序继续在系统托盘运行，仍会按时提醒 |
| 彻底退出 | 结束进程，不再接收提醒 |
| 取消 | 保持窗口打开 |

- 勾选「记住我的选择」后，后续关闭按记忆执行，不再询问。
- 可在「设置 → 关闭窗口行为」查看当前记忆值，并点「重新询问」重置。

## 新建任务卡片（悬浮）

- 「新建任务」表单**默认隐藏**，点击顶部「新建任务」按钮后以**悬浮卡片**形式出现在窗口顶部。
- 点击卡片外遮罩、右上角关闭按钮或按 `Esc` 均可收起。
- 任务列表始终占满内容区宽度。

## 外观与主题色

- **外观**：设置 → 外观，可选「跟随系统 / 浅色 / 深色」。
  选「跟随系统」时，Windows 切换深/浅色会**实时同步**（由主进程 `nativeTheme` 推送，
  比渲染进程的 `matchMedia` change 事件可靠——后者在 Electron 中常不触发）。
- **主题色**：设置 → 主题色，提供 7 种预设（靛蓝 / 紫罗兰 / 青碧 / 翡翠 / 琥珀 / 玫红 / 石墨）。
  主窗口与灵动岛会同步应用；每种预设都带浅色与深色两套派生色，保证对比度。

## 分类管理

- 侧边栏「按分类」右侧的**管理**按钮，或「设置 → 分类管理」，均可编辑分类。
- 支持**新增**（输入名称后回车或点添加）、**重命名**（直接改输入框）、**删除**。
- 删除分类时，该分类下的任务会自动归入「其他」；分类数据随设置一起持久化。

## 数据存储位置

- 数据目录：`%APPDATA%\smart-todo-desktop\`
  - `todo-data.json` —— 任务与设置数据
  - `window.json` —— 窗口位置记忆

## 项目结构

```
ToDoDesktopApp/
├── 智能待办-便携版.exe   # 免安装便携版（直接交付）
├── main.js               # 主进程：窗口/托盘/通知/开机自启/单实例
├── preload.js            # 安全桥接 API
├── renderer/
│   ├── index.html        # 界面（SVG 图标）
│   ├── app.js            # 渲染逻辑
│   └── styles.css        # 样式（明暗主题）
├── assets/icon.png       # 应用/托盘/通知图标
├── package.json
└── dist/                 # 打包产物
```

## 系统托盘使用

- **单击**托盘图标：打开主窗口
- **双击**托盘图标：打开主窗口
- **托盘右键菜单**：
  - 打开主界面
  - 新建任务 / 显示全部任务
  - 开机自动启动（勾选）
  - 退出

## 系统级能力实现

- **后台常驻**：主进程捕获窗口 `close` 事件，关闭时隐藏而非退出；托盘持引用不释放
- **开机自启（避免 temp 路径）**：便携版每次运行会自解压到 `%TEMP%` 临时目录。
  因此不用 `app.setLoginItemSettings()`（会把自启项指向重启即失效的临时路径），
  而是把便携 exe 复制一份到固定目录 `%APPDATA%\SmartTodoDesktop\`，
  通过注册表 `HKCU\...\Run` 键指向该固定副本。开机从固定路径启动，不受 `%TEMP%` 清理影响。
- **单实例**：`app.requestSingleInstanceLock()`，重复启动聚焦已有窗口
- **系统通知**：主进程 `new Notification()`，点击通知唤醒窗口
- **数据持久化**：JSON 文件写入用户数据目录
